apiVersion: v1
kind: Service
metadata:
  name: multiport-broken
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 80
    - port: 9090
      targetPort: 9090
