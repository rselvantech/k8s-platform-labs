apiVersion: v1
kind: Service
metadata:
  name: backend-svc-typo
spec:
  type: ClusterIP
  selector:
    app: breakfix-backedn # typo: should be "breakfix-backend"
  ports:
    - port: 9090
      targetPort: 5678
