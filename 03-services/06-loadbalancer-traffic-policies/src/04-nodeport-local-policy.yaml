apiVersion: v1
kind: Service
metadata:
  name: backend-traffic-local
spec:
  type: NodePort
  externalTrafficPolicy: Local
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31200
